<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myrentalapp">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReturnToInventoryActivity" />
        <activity android:name=".InventoryManagementActivity" />
        <activity android:name=".SalesPersonReservationSearchActivity" />
        <activity android:name=".ReadBookingsDataActivity" />
        <activity android:name=".ManageBookingsActivity" />
        <activity android:name=".ReadCustomerData" />
        <activity android:name=".ManageCustomerActivity" />
        <activity android:name=".SelectionCriteriaActivity" />
        <activity android:name=".RentalFinalizeActivity" />
        <activity android:name=".AddNewInventoryActivity" />
        <activity android:name=".ClientInfoActivity" />
        <activity android:name=".RentalSelectionActivity" />
        <activity android:name=".UpdateDataActivity" />
        <activity android:name=".ReadAllData" />
        <activity android:name=".ClientActivity" />
        <activity android:name=".SalesActivity" />
        <activity android:name=".ManagerActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>